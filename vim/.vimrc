set nocompatible

" Enable filetype plugins
filetype indent on
filetype plugin on

" Enable syntax highlighting
syntax on

" Allow hiding unsaved buffers
set hidden

" Map jk to ESC
inoremap jk <esc>

" Map ; to :
nmap ; :
